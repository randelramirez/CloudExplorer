# Ubuntu packages

`scripts/publish-ubuntu.sh` writes Ubuntu portable archives or Snap packages and SHA-256 checksums here.

See the [Ubuntu publishing and installation guide](../../docs/platforms/UBUNTU.md).
